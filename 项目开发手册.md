# 瀚星 Super Image — 企业级 AI 图片工作台 · 项目开发手册

> **文档版本**：v1.1 ｜ **生成日期**：2026-08-07
> **项目代号**：Hanxing-Super-image（瀚星 Super Image / 多租户企业版）
> **前身项目**：Hanxing-image（单租户内部版，作为功能参考，不迁移数据）
>
> **更新记录**
> - v1.1（2026-08-07）：根据 R5/R6 确认与遗漏点复查，① 简化用户-企业关系（用户单一归属，去掉 memberships 多对多表）；② 明确权限组绑定企业；③ 新增企业级模块开关、成员管理权、并发租户限额；④ 补齐索引设计、存储/Redis 命名规范、登录限流与审计、测试策略、运维（健康检查/备份/监控）；⑤ 误删的 sidebar-08 企业切换器改为企业标识展示。
> - v1.0（2026-08-07）：初版，确立 17 项根本决策。

---

## 〇、TL;DR（一句话定位）

> 在全新的 Next.js 全栈架构上，重构瀚星 AI 图片工作台为 **平台级多租户 SaaS**：每家企业拥有独立租户、独立的共享积分池、独立的管理员与用户；业务上把原"自由创作 + 项目创作"合并为**统一的 AI 生图页面**，沿用 Pulse 光效对话框；UI 全程使用 shadcn 原生组件（**黑夜模式**），主页用 `sidebar-08` 模板，登录页用 `login-04` 模板；底层从 SQLite 升级为 **PostgreSQL + Redis**，Docker Compose 自托管。

---

## 一、决策记录（Decision Log）

下表是已与产品负责人确认的所有根本性决策，**后续所有设计与实现不得违背**：

| # | 决策项 | 选定方案 | 影响 |
|---|---|---|---|
| D1 | 技术架构 | **Next.js 全栈重写**（非沿用 Vite SPA） | 前端整体重写，最大复用价值在"业务逻辑/数据模型"而非代码 |
| D2 | 路由系统 | **App Router**（服务端组件 + Server Actions + Route Handlers） | 契合 shadcn 原生模板、RSC、Server Components |
| D3 | 数据库 | **PostgreSQL**（替换 SQLite） | schema 全新设计，多租户字段 |
| D4 | 缓存 | **Redis**（会话/限流/队列状态/热点缓存） | 摆脱 in-process 队列的单实例限制 |
| D5 | ORM | **Drizzle ORM** + drizzle-kit 迁移 | SQL-like、类型安全、零运行时开销 |
| D6 | 认证 | **Auth.js v5（NextAuth）+ Credentials 账号密码** | 为未来接入企业 SSO（企业微信/钉钉/OIDC）预留 Provider 扩展位 |
| D7 | 多租户 | **平台级多租户，行级数据隔离**（所有业务表带 `enterprise_id`） | 超级管理员全局；企业管理员管本企业；用户只见本企业数据 |
| D8 | 积分粒度 | **企业级共享积分池**（单一永久积分余额，无每日刷新） | 企业充值 → 全员共享扣减；无 daily_credits |
| D9 | 积分合并 | **创作积分 + 项目积分 → 统称"积分"（credits）** | 单一字段，统一计费 |
| D10 | UI 组件 | **shadcn 原生 Next.js 版**，全程黑夜模式 | `npx shadcn@latest add`，禁止重复造轮子 |
| D11 | 主页模板 | **`sidebar-08`**（可折叠侧边栏 + 团队切换器 + nav-user） | 团队切换器天然适配"企业切换" |
| D12 | 登录模板 | **`login-04`**（社交登录位 + 表单） | 暂不实现的第三方登录保留占位样式，后期接入 |
| D13 | 创作页 | **自由创作 + 项目创作 → 合并为单一"创作"页**，Pulse 光效对话框 | 去除 source=creative/project 区分，仅一种积分计费 |
| D14 | 业务模块 | v1 保留：**统一创作页 + 资产管理 + 批量生图 + 商品主图** | 全部业务模块均在 v1 范围内 |
| D15 | 数据迁移 | **全新开始，不迁移旧数据** | 无需迁移脚本，schema 自由设计 |
| D16 | 部署 | **Docker Compose 自托管**（Next.js + PG + Redis） | 私有化交付友好，沿用国内镜像源策略 |
| D17 | 推进顺序 | **基础架构优先**（地基稳，再逐模块） | 里程碑 M1 先打通全栈骨架 |
| D18 | 用户归属 | **一个用户名只属一家企业，不可跨企业**；用户名即登录账号（全局唯一），昵称可重复 | 去掉 memberships 多对多表，`users` 表直接挂 `enterprise_id`；删除 `active_enterprise_id`；sidebar-08 企业切换器改为**纯展示当前企业标识**（不可点击切换） |
| D19 | 用户分配 | **用户由超级管理员创建并分配企业**；企业管理员（owner/admin）负责本企业内**成员增删、改角色** | 超管建账号 + 分配企业 + 设初始角色；之后日常成员管理权下放给企业管理员 |
| D20 | 企业注册 | **v1 不开放企业自助注册，仅超管创建企业** | `/platform/enterprises` 是唯一创建入口 |
| D21 | 权限组 | **保留权限组概念，但绑定到企业**（每企业可自定义多个权限组，如"设计组/运营组"）；用户分配到组，组决定 `allowed_models` / `allowed_pages` / `max_concurrent` / `priority` | `permission_groups.enterprise_id` 非空；种子数据为每家企业预置一个默认组 |
| D22 | 模块权限 | **企业级模块开关**：超管为每家企业配置 `enterprises.enabled_modules`（如 `[create,assets,workspace,product]`）；企业内所有成员都能访问企业已开通的模块 | 简单一致，不再按角色细分；组成员的 `allowed_pages` 在企业已开通模块内进一步限定 |

---

## 二、技术栈总览

### 2.1 技术选型一览

| 层 | 技术 | 版本目标 | 用途 |
|---|---|---|---|
| **框架** | Next.js | 15.x（App Router） | 全栈框架，RSC + Route Handlers + Server Actions |
| **语言** | TypeScript | 5.x | 严格模式 |
| **包管理** | pnpm | 9.x | 比 npm 更快、磁盘节省（替代旧项目的 npm） |
| **运行时** | Node.js | 22.x（Alpine） | 与旧项目 Docker 基础镜像对齐 |
| **数据库** | PostgreSQL | 16.x | 多租户主存储 |
| **ORM** | Drizzle ORM + drizzle-kit | 最新稳定 | schema 定义、迁移、类型安全查询 |
| **缓存/队列** | Redis | 7.x | 会话、限流、任务队列状态、热点缓存 |
| **认证** | Auth.js v5（next-auth） | 5.x（beta→stable） | Credentials Provider，预留 OAuth |
| **UI 组件** | shadcn/ui（Next.js 原生） | 最新 | sidebar-08、login-04、及其余基础组件 |
| **样式** | Tailwind CSS | v4（shadcn 当前默认） | 黑夜模式（dark 作为默认主题） |
| **图标** | lucide-react | 最新 | shadcn 默认图标库 |
| **动效** | framer-motion | 12.x | 创作页过渡、Pulse 光效配合 |
| **表单校验** | zod + react-hook-form | 最新 | 前后端共享 schema |
| **图片处理** | sharp | 最新 | 缩略图（沿用旧项目） |
| **对象存储** | 腾讯云 COS（可选）/ 本地 | — | 与旧项目一致，双后端可切换 |
| **加密** | Node crypto（AES-256-GCM） | 内置 | API Key、COS SecretKey 加密落库 |
| **部署** | Docker Compose | — | 三服务编排 |

### 2.2 关于"黑夜模式"

- **默认即黑夜**：`<html className="dark">` 由根布局直接锁定，配合 `next-themes` 做 Provider，默认主题为 `dark`，用户可切换（但首次进入强制 dark）。
- shadcn 的 `sidebar-08` / `login-04` 模板本身已内置完整的暗色 token，我们仅强制默认值。
- 所有自定义样式遵循 shadcn 的 CSS 变量（`--background`、`--foreground`、`--card`、`--border` 等），**禁止硬编码颜色值**。

---

## 三、目录结构（目标态）

```
Hanxing-Super-image/
├── .env.example                    # 所有环境变量模板（含说明）
├── .env                            # 本地开发（不入库）
├── .gitignore
├── README.md
├── 项目开发手册.md                  # 本文档
├── package.json                    # pnpm 工作区根
├── pnpm-lock.yaml
├── next.config.ts                  # Next.js 配置
├── tsconfig.json
├── tailwind.config.ts              # Tailwind v4
├── postcss.config.mjs
├── components.json                 # shadcn 配置（Next.js 原生，rsc: true）
├── drizzle.config.ts               # drizzle-kit 配置
├── docker-compose.yml              # 三服务编排（app + postgres + redis）
├── Dockerfile                      # Next.js 多阶段构建
├── docker/
│   ├── daemon.json                 # 国内 Docker 镜像源（沿用旧项目策略）
│   └── mirror-sources.md
│
├── drizzle/                        # 迁移目录（drizzle-kit generate 输出）
│   ├── 0000_init.sql
│   ├── 0001_enterprise.sql
│   └── meta/
│
├── public/
│   ├── logo.svg
│   └── favicon.svg
│
└── src/
    ├── app/                        # App Router 路由
    │   ├── (auth)/                 # 登录/注册路由组（无侧边栏布局）
    │   │   ├── login/
    │   │   │   └── page.tsx        # 基于 login-04 模板的登录页
    │   │   └── layout.tsx
    │   │
    │   ├── (dashboard)/            # 主应用路由组（带 sidebar-08 侧边栏）
    │   │   ├── layout.tsx          # 嵌入 AppSidebar（sidebar-08）
    │   │   ├── page.tsx            # 首页 → 重定向到 /create
    │   │   ├── create/             # ★ 统一创作页（合并自由/项目创作）
    │   │   │   └── page.tsx
    │   │   ├── assets/             # 资产管理
    │   │   │   └── page.tsx
    │   │   ├── workspace/          # 批量生图工作台
    │   │   │   └── page.tsx
    │   │   ├── product/            # 商品主图
    │   │   │   └── page.tsx
    │   │   └── settings/           # 用户个人设置（个人资料、密码）
    │   │       └── page.tsx
    │   │
    │   ├── (admin)/                # 企业管理路由组
    │   │   ├── layout.tsx
    │   │   └── admin/
    │   │       ├── page.tsx        # 企业管理总览
    │   │       ├── users/          # 企业用户管理
    │   │       ├── models/         # 模型/API 配置
    │   │       ├── credits/        # 企业积分充值与流水
    │   │       ├── logs/           # 企业操作日志
    │   │       └── workspace/      # 工作台模板/对话API管理
    │   │
    │   ├── (superadmin)/           # 平台超级管理路由组
    │   │   ├── layout.tsx
    │   │   └── platform/
    │   │       ├── page.tsx        # 平台总览
    │   │       ├── enterprises/    # ★ 企业管理（创建/配置企业）
    │   │       ├── users/          # 平台用户总览
    │   │       └── system/         # 系统设置（存储、全局参数）
    │   │
    │   ├── api/                    # Route Handlers（无法用 Server Action 的场景）
    │   │   ├── auth/
    │   │   │   └── [...nextauth]/
    │   │   │       └── route.ts    # Auth.js 入口
    │   │   ├── upload/
    │   │   │   └── route.ts        # 文件上传（参考图）
    │   │   ├── image/
    │   │   │   ├── thumb/route.ts  # 缩略图
    │   │   │   └── proxy/route.ts  # 图片代理（防 SSRF）
    │   │   ├── tasks/
    │   │   │   └── [id]/
    │   │   │       └── result/     # 任务结果轮询端点（SSE 备选）
    │   │   └── webhooks/           # AI 服务异步回调（预留）
    │   │
    │   ├── layout.tsx              # 根布局（ThemeProvider、Toaster）
    │   └── globals.css             # 全局样式 + shadcn 变量 + dark 默认
    │
    ├── components/
    │   ├── ui/                     # shadcn 生成的基础组件（button/dialog/...）
    │   ├── sidebar/                # ★ sidebar-08 模板组件
    │   │   ├── app-sidebar.tsx
    │   │   ├── enterprise-badge.tsx     # ★ 企业标识展示（非切换器，R5 单一归属）
    │   │   ├── nav-main.tsx
    │   │   ├── nav-user.tsx
    │   │   └── team-switcher.tsx   # 模板原始，保留参考
    │   ├── login/                  # ★ login-04 模板组件
    │   │   └── login-form.tsx
    │   ├── create/                 # 统一创作页组件
    │   │   ├── generate-dialog.tsx # ★ Pulse 光效对话框
    │   │   ├── model-selector.tsx
    │   │   ├── size-selector.tsx
    │   │   ├── reference-image-upload.tsx
    │   │   ├── task-detail.tsx
    │   │   ├── history-sidebar.tsx
    │   │   └── border-beam.tsx     # Pulse 边框光效（参考 Jakub Antalik）
    │   ├── workspace/              # 批量生图组件（沿用并重构）
    │   ├── product/                # 商品主图组件
    │   ├── admin/                  # 企业管理后台组件
    │   ├── superadmin/             # 平台管理组件
    │   └── shared/                 # 共享业务组件
    │
    ├── db/                         # 数据层
    │   ├── schema/                 # ★ Drizzle schema（按域拆分）
    │   │   ├── index.ts
    │   │   ├── auth.ts             # users（含 enterpriseId/enterpriseRole/groupId）/ sessions / accounts / verificationTokens
    │   │   ├── enterprise.ts       # enterprises / permission_groups（★ 绑定企业）
    │   │   ├── credits.ts          # credit_transactions（余额在 enterprises.creditsBalance）
    │   │   ├── audit.ts            # login_logs（★ 新增审计表）
    │   │   ├── models.ts           # models
    │   │   ├── tasks.ts            # generation_tasks / api_call_logs / pinned_tasks
    │   │   ├── workspace.ts        # workspace_tasks/prompt_cards/card_images/...
    │   │   ├── product.ts          # product_template_groups/templates/...
    │   │   └── system.ts           # system_settings
    │   ├── client.ts               # Drizzle 实例（单例）
    │   ├── relations.ts            # 关系定义
    │   └── seed.ts                 # 种子数据（超管 + 默认企业 + 默认权限组）
    │
    ├── lib/                        # 基础设施
    │   ├── auth/
    │   │   ├── config.ts           # Auth.js 配置（providers、callbacks）
    │   │   ├── session.ts          # 会话辅助（获取当前用户/企业）
    │   │   └── permissions.ts      # 角色/权限校验工具
    │   ├── ai/
    │   │   ├── grs.ts              # GRS 中转站适配器（沿用旧项目）
    │   │   ├── jimeng.ts           # 即梦适配器（沿用旧项目）
    │   │   ├── image-model-config.ts # 请求体构造、能力校验
    │   │   └── index.ts            # 统一调度入口 callImageAPI()
    │   ├── queue/
    │   │   ├── task-queue.ts       # ★ 基于 Redis 的分布式任务队列
    │   │   └── chat-queue.ts       # 对话任务队列
    │   ├── storage/
    │   │   ├── cos.ts              # 腾讯云 COS
    │   │   ├── local.ts            # 本地存储
    │   │   └── index.ts            # 统一接口（按 system_settings 切换）
    │   ├── crypto.ts               # AES-256-GCM（沿用旧项目）
    │   ├── redis.ts                # Redis 客户端单例
    │   ├── rate-limit.ts           # 基于 Redis 的限流（登录 + 生图）
    │   ├── audit.ts                # 审计日志写入（login_logs）
    │   ├── env.ts                  # zod 校验的环境变量
    │   ├── utils.ts                # cn() 等通用工具
    │   └── constants.ts
    │
    ├── server/                     # ★ 服务端业务逻辑（被 Server Actions / Route Handlers 调用）
    │   ├── actions/                # ★ Server Actions（"use server"）
    │   │   ├── auth.ts             # 登录、登出（配合 Auth.js）
    │   │   ├── create.ts           # 提交生图任务、重试
    │   │   ├── workspace.ts        # 工作台操作
    │   │   ├── product.ts          # 商品主图
    │   │   ├── assets.ts           # 资产管理（收藏、删除、导出）
    │   │   ├── credits.ts          # 充值、查询流水
    │   │   ├── admin-users.ts      # 企业用户 CRUD
    │   │   ├── admin-models.ts     # 模型 CRUD
    │   │   └── platform.ts         # 平台/企业管理
    │   ├── services/               # 纯服务端业务逻辑（非 Action）
    │   │   ├── credits-service.ts  # ★ 积分原子扣减/退款（企业级）
    │   │   ├── generation-service.ts
    │   │   └── workspace-ai.ts     # 提示词裂变/翻译/深化（沿用旧项目）
    │   └── middleware.ts           # 权限中间件逻辑
    │
    ├── hooks/                      # React Hooks（前端）
    │   ├── use-mobile.ts
    │   └── use-generation.ts       # 沿用并重构 useGeneratePage
    │
    ├── types/                      # 全局类型
    │   ├── api.ts
    │   ├── domain.ts
    │   └── next-auth.d.ts          # 扩展 Auth.js Session 类型
    │
    ├── __tests__/                  # ★ 测试（Vitest + testcontainers）
    │   ├── unit/                   # 单元测试（crypto、image-model-config、permissions）
    │   └── integration/            # 集成测试（credits 并发扣减、租户隔离）
    │
    └── middleware.ts               # ★ Next.js 中间件（路由守卫、租户解析）
```

**关键设计点**：

1. **路由组（Route Groups）** `(auth)` / `(dashboard)` / `(admin)` / `(superadmin)` 各自独立布局，互不污染。
2. **Server Actions 集中放 `src/server/actions/`**，业务逻辑下沉到 `src/server/services/`，Route Handlers 只处理必须走 HTTP 的场景（上传、SSE、Webhook）。
3. **schema 按域拆分**到 `src/db/schema/`，每个文件一个聚合根，`index.ts` 汇总导出。
4. **AI 适配器、加密、队列、存储** 全部下沉到 `src/lib/`，是服务端专用代码（确保不被打包到客户端）。

---

## 四、数据模型设计（Drizzle Schema 草案）

> 设计原则：**所有业务表带 `enterpriseId` 做租户隔离**；积分按企业聚合；用 Drizzle 关系定义导航。

### 4.1 认证与租户域（★ R5 简化版：用户单一归属）

> **决策 D18**：一个用户名只属一家企业，不可跨企业。因此**不需要 memberships 多对多表**，`users` 表直接挂 `enterpriseId` + `enterpriseRole`。用户名（登录账号）全局唯一；昵称可重复。

```ts
// src/db/schema/enterprise.ts
export const enterprises = pgTable('enterprises', {
  id: uuid('id').defaultRandom().primaryKey(),
  name: varchar('name', { length: 100 }).notNull(),                  // 企业名称
  slug: varchar('slug', { length: 50 }).notNull().unique(),            // URL 友好标识
  logoUrl: text('logo_url'),
  status: enterpriseStatusEnum('status').default('active').notNull(), // active | suspended
  creditsBalance: integer('credits_balance').default(0).notNull(),    // ★ 企业共享积分池
  enabledModules: jsonb('enabled_modules').$type<ModuleName[]>()      // ★ 企业级模块开关（D22）
    .default(['create', 'assets']).notNull(),                          // 默认只开通创作+资产
  maxConcurrent: integer('max_concurrent').default(5).notNull(),       // ★ 企业级并发上限（D新增）
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(),
})

// src/db/schema/auth.ts
export const users = pgTable('user', {
  id: uuid('id').defaultRandom().primaryKey(),
  username: varchar('username', { length: 50 }).notNull().unique(),    // ★ 全局唯一（登录账号）
  email: varchar('email', { length: 255 }),
  name: varchar('name', { length: 100 }),                              // 昵称（可重复）
  passwordHash: varchar('password_hash', { length: 255 }).notNull(),
  image: text('image'),                                                 // 头像 URL
  isSuperAdmin: boolean('is_super_admin').default(false).notNull(),     // ★ 平台超管（超管无企业归属）
  enterpriseId: uuid('enterprise_id').references(() => enterprises.id), // ★ 用户唯一归属的企业（超管为 NULL）
  enterpriseRole: enterpriseRoleEnum('enterprise_role')                 // ★ 企业内角色（owner|admin|member）
    .default('member').notNull(),
  groupId: uuid('group_id').references(() => permissionGroups.id),      // ★ 所属权限组（D21）
  status: userStatusEnum('status').default('active').notNull(),
  lastLoginAt: timestamp('last_login_at', { withTimezone: true }),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(),
})

// ★ 删除了 v1.0 的 memberships 表（用户单一归属，不再多对多）
// ★ 删除了 activeEnterpriseId（用户只有一个企业，无需"活跃企业"字段）

// Auth.js 要求的标准表
export const accounts = pgTable('account', { /* id, userId, provider, providerAccountId, ... */ })
export const sessions = pgTable('session', { /* id, sessionToken, userId, expires */ })
export const verificationTokens = pgTable('verificationtoken', { /* ... */ })

// src/db/schema/enterprise.ts（权限组，★ D21 绑定企业）
export const permissionGroups = pgTable('permission_group', {
  id: uuid('id').defaultRandom().primaryKey(),
  enterpriseId: uuid('enterprise_id').notNull().references(() => enterprises.id), // ★ 非空，绑定企业
  name: varchar('name', { length: 100 }).notNull(),                    // 组名（如"设计组""运营组"）
  description: text('description'),
  allowedModels: jsonb('allowed_models').$type<string[]>().default([]).notNull(), // 模型 id 白名单（空=企业全部已开通模型）
  allowedPages: jsonb('allowed_pages').$type<string[]>().default([]).notNull(),   // 页面白名单（在企业 enabledModules 范围内）
  maxConcurrent: integer('max_concurrent').default(2).notNull(),       // 组级并发上限（≤ 企业 maxConcurrent）
  priority: integer('priority').default(0).notNull(),                  // 任务优先级（数字大先执行）
  isDefault: boolean('is_default').default(false).notNull(),           // 新用户默认组（每企业一个）
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
}, (t) => ({
  uniqDefault: unique('one_default_per_enterprise')
    .on(t.enterpriseId).where(sql`is_default = true`), // 每企业至多一个默认组
}))
```

**说明**：

- **三层角色清晰**：`users.isSuperAdmin`（平台超管，无企业归属）/ `users.enterpriseRole`（企业内 owner|admin|member）/ `users.groupId`（权限组，决定能用哪些模型/页面/并发）。
- **超管的 `enterpriseId` 为 NULL**；普通用户 `enterpriseId` 非空，且一旦分配**不可跨企业**（如需调换，由超管改 `enterpriseId`）。
- **权限组绑定企业**（D21）：每家企业可自定义多个组；新建企业时种子一个默认组（`isDefault=true`）。
- **模块开关与页面白名单的关系**（D22 + D21）：企业开通 `enabledModules`（超管设）；权限组的 `allowedPages` 只能在企业已开通模块内进一步收窄（如"运营组"看不到 workspace）。

### 4.2 积分域（单一积分 + 流水）

```ts
// src/db/schema/credits.ts
// 余额：直接在 enterprises.creditsBalance（见 4.1），不单独建表
// 流水：所有积分变动都落审计表

export const creditTransactions = pgTable('credit_transactions', {
  id: uuid('id').defaultRandom().primaryKey(),
  enterpriseId: uuid('enterprise_id').notNull().references(() => enterprises.id),
  userId: uuid('user_id').references(() => users.id),             // 触发者（充值时为管理员）
  type: creditTxTypeEnum('type').notNull(),                       // recharge | consumption | refund | adjustment
  amount: integer('amount').notNull(),                            // 正=充值/退款, 负=扣减
  balanceAfter: integer('balance_after').notNull(),               // 变动后余额（审计快照）
  taskId: uuid('task_id').references(() => generationTasks.id),   // 关联任务（消费/退款时）
  remark: text('remark'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
})
```

**积分扣减必须用事务 + 行锁**：

```ts
// src/server/services/credits-service.ts（核心，伪代码）
export async function deductCredits(enterpriseId, amount, taskId, userId) {
  return await db.transaction(async (tx) => {
    const [row] = await tx
      .select({ balance: enterprises.creditsBalance })
      .from(enterprises)
      .where(eq(enterprises.id, enterpriseId))
      .for('update')  // ★ 行锁，防止并发超扣
    if (row.balance < amount) throw new CreditsInsufficientError()
    const [updated] = await tx.update(enterprises)
      .set({ creditsBalance: sql`${enterprises.creditsBalance} - ${amount}` })
      .where(eq(enterprises.id, enterpriseId))
      .returning({ balance: enterprises.creditsBalance })
    await tx.insert(creditTransactions).values({
      enterpriseId, userId, type: 'consumption', amount: -amount,
      balanceAfter: updated.balance, taskId,
    })
    return updated.balance
  })
}
```

### 4.3 模型与任务域

```ts
// src/db/schema/models.ts
export const models = pgTable('model', {
  id: uuid('id').defaultRandom().primaryKey(),
  enterpriseId: uuid('enterprise_id'),  // ★ NULL = 平台级共享模型;非 NULL = 企业私有模型
  name: varchar('name', { length: 100 }).notNull(),
  displayName: varchar('display_name', { length: 200 }).notNull(),
  apiEndpoint: text('api_endpoint').notNull(),
  apiKeyEncrypted: text('api_key_encrypted').notNull(),
  apiFormat: apiFormatEnum('api_format').default('grs').notNull(), // grs | jimeng
  extraConfig: jsonb('extra_config').$type<ModelExtraConfig>(),
  costPerImage: integer('cost_per_image').default(1).notNull(),
  supportedSizes: jsonb('supported_sizes'),
  // 页面可见性（沿用旧项目四字段）
  visibleInCreate: boolean('visible_in_create').default(true).notNull(),   // ★ 统一创作页
  visibleInWorkspace: boolean('visible_in_workspace').default(false).notNull(),
  visibleInProduct: boolean('visible_in_product').default(false).notNull(),
  supportsReferenceImage: boolean('supports_reference_image').default(false).notNull(),
  maxReferenceImages: integer('max_reference_images').default(0).notNull(),
  maxConcurrent: integer('max_concurrent').default(2).notNull(),
  maxRetries: integer('max_retries').default(2).notNull(),
  apiTimeout: integer('api_timeout').default(120).notNull(),
  taskTimeout: integer('task_timeout').default(300).notNull(),
  isActive: boolean('is_active').default(true).notNull(),
  iconUrl: text('icon_url'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
})
// 约束：UNIQUE(name, api_endpoint, enterprise_id)
```

**关键变化（对照旧项目）**：

- 旧项目 `visible_in_generate` + `visible_in_canvas` 两个字段 → **合并为单一 `visibleInCreate`**（对应创作页合并）。
- `enterpriseId` 允许 NULL，代表**平台预置模型**（所有企业可见）；非 NULL 为企业私有模型。
- 废弃旧的 `credits_type` 字段（单一积分后无意义）。

```ts
// src/db/schema/tasks.ts
export const generationTasks = pgTable('generation_task', {
  id: uuid('id').defaultRandom().primaryKey(),
  enterpriseId: uuid('enterprise_id').notNull().references(() => enterprises.id), // ★ 租户隔离
  userId: uuid('user_id').notNull().references(() => users.id),
  modelId: uuid('model_id').notNull().references(() => models.id),
  prompt: text('prompt').notNull(),
  imageSize: varchar('image_size', { length: 30 }),               // 1024x1024
  imageCount: integer('image_count').default(1).notNull(),
  status: taskStatusEnum('status').default('queued').notNull(),   // queued|processing|completed|failed
  taskType: taskTypeEnum('task_type').default('normal').notNull(), // normal|workspace_single|workspace_batch|product
  source: varchar('source', { length: 30 }).default('create').notNull(), // create|workspace|product（去 creative/project）
  priority: integer('priority').default(0).notNull(),
  creditsCharged: integer('credits_charged').default(0).notNull(), // ★ 统一积分（无 type）
  resultImages: jsonb('result_images').$type<string[]>(),
  referenceImages: jsonb('reference_images').$type<string[]>(),
  errorMessage: text('error_message'),
  retryCount: integer('retry_count').default(0).notNull(),
  retryErrors: jsonb('retry_errors'),
  templateInfo: jsonb('template_info'),
  taskUuid: varchar('task_uuid', { length: 64 }),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
  startedAt: timestamp('started_at', { withTimezone: true }),
  completedAt: timestamp('completed_at', { withTimezone: true }),
})

export const apiCallLogs = pgTable('api_call_log', { /* 沿用旧项目字段 */ })
export const pinnedTasks = pgTable('pinned_task', { /* 沿用旧项目字段 */ })
```

### 4.4 工作台域 / 商品主图域 / 系统设置

这三块沿用旧项目的表结构，**只做三处统一改造**：

1. 所有表加 `enterpriseId` 字段做租户隔离；
2. 涉及 `creative_credits`/`project_credits` 的逻辑改为统一 `credits`；
3. JSON 字段统一用 PostgreSQL `jsonb`（旧项目 SQLite 是 text）。

（具体表定义在 M2/M5/M6 阶段按旧项目 `migrate.ts` 1:1 平移 + 上述改造，本手册不重复罗列。）

### 4.5 索引设计（★ 新增，多租户性能保障）

> 多租户铁律：所有业务表带 `enterprise_id`，因此**每张业务表都必须在 `(enterprise_id, ...)` 上建组合索引**，否则跨企业查询会全表扫。

```ts
// Drizzle 索引定义示例（schema 文件内）
// generation_task：按企业 + 状态查队列、按企业 + 用户查历史
index('gt_ent_status').on(table.enterpriseId, table.status),
index('gt_ent_user_created').on(table.enterpriseId, table.userId, table.createdAt),

// permission_group：按企业列组
index('pg_ent').on(table.enterpriseId),

// credit_transactions：按企业 + 时间倒序查流水
index('ct_ent_created').on(table.enterpriseId, table.createdAt),

// generation_task.taskUuid：唯一索引（轮询查询单任务）
uniqueIndex('gt_uuid').on(table.taskUuid),

// user.username：唯一索引（已由 .unique() 保证）
// enterprises.slug：唯一索引（已由 .unique() 保证）
```

**索引清单（强制）**：`users.enterpriseId`、`generation_task.(enterpriseId,status)`、`generation_task.(enterpriseId,userId,createdAt)`、`credit_transactions.(enterpriseId,createdAt)`、`permission_group.enterpriseId`、所有 workspace/product 三层表的 `(enterpriseId, parentId)`。

---

## 五、关键架构设计

### 5.1 多租户与权限模型（★ R5 单一归属 + 三层角色 + 权限组）

```
┌──────────────────────────────────────────────────────────┐
│ 平台超管（is_super_admin=true，无企业归属）                │
│  - 创建/停用企业，为企业充值                                │
│  - 创建用户并分配到企业（初始角色）★ D18/D19                │
│  - 配置企业开通模块（enabledModules）★ D22                  │
│  - 管理平台预置模型（enterpriseId=NULL）                    │
│  - 全局系统设置                                            │
└────────────────────────┬─────────────────────────────────┘
                         │ 1:N（一家企业多个用户，用户不可跨企业）
┌────────────────────────▼─────────────────────────────────┐
│ 企业（enterprise）                                        │
│  - 独立积分池（creditsBalance，企业全员共享）★ D8          │
│  - 模块开关（enabledModules）★ D22                         │
│  - 企业级并发上限（maxConcurrent）                         │
│  - 企业主/管理员（enterpriseRole = owner|admin）★ D19      │
│    - 本企业内增删成员、改成员角色、分配权限组                │
│    - 配置企业私有模型、权限组、工作台模板                   │
│  - 企业成员（enterpriseRole = member）                     │
│    - 使用企业积分生图、查看自己的任务                        │
│    - 归属一个权限组（groupId）决定能用哪些模型/页面/并发    │
└──────────────────────────────────────────────────────────┘
```

**四层鉴权维度**（每个请求依次校验）：

| 维度 | 字段 | 决定 |
|---|---|---|
| 平台身份 | `users.isSuperAdmin` | 能否进 `/platform/*` |
| 企业归属 | `users.enterpriseId` | 数据隔离作用域（超管为 NULL，跳过） |
| 企业角色 | `users.enterpriseRole` | 能否进 `/admin/*`（owner/admin 可进） |
| 权限组 | `users.groupId` → `allowedModels`/`allowedPages`/`maxConcurrent` | 能用哪些模型、能看到哪些页面、个人并发上限 |

**鉴权实现**：

- **Next.js middleware（`src/middleware.ts`）**：拦截所有路由，校验 Auth.js session；读取 `user.enterpriseId` + `isSuperAdmin` + `enterpriseRole`；按角色 + 路由前缀做粗粒度守卫（`/platform/*` 必须 isSuperAdmin；`/admin/*` 必须 owner/admin；普通页面校验企业模块开关）。
- **Server Action 内细粒度校验**：每个 Action 调用 `getCurrentUserContext()` 拿到 `(user, enterprise, group)` 三元组，再做业务级校验（模型是否在 `group.allowedModels`、页面是否在企业 `enabledModules`）。
- **数据级隔离**：所有查询强制带 `WHERE enterprise_id = $user.enterpriseId`，通过 `getCurrentEnterpriseScope()` 工具函数注入，**禁止裸查**。

**模块访问校验链**（每次进入业务页/调用生图 Action）：

```
企业 enabledModules 是否含目标模块？  → 否：403
  └ 是 → 权限组 allowedPages 是否含目标页？ → 否：403
          └ 是 → 放行
```

### 5.2 企业标识展示（★ R5：非切换器）

旧项目侧边栏底部是静态用户卡。新项目用 `sidebar-08` 的 **Team Switcher 组件改为"企业标识展示"**（用户单一归属，**不可切换**）：

- 侧边栏顶部显示当前用户的归属企业：企业 logo + 企业名称（只读展示，无下拉）；
- 超管登录后此区域显示"平台管理"字样（超管无企业归属），点击进入 `/platform`；
- **不再有 `switchEnterprise()` Action**（用户不可跨企业）；
- 侧边栏 `nav-main` 的菜单项按 `enterprise.enabledModules` ∩ `group.allowedPages` 过滤。

### 5.3 Auth.js v5 配置要点

```ts
// src/lib/auth/config.ts（要点，非完整代码）
import NextAuth from 'next-auth'
import Credentials from 'next-auth/providers/credentials'
import { DrizzleAdapter } from '@auth/drizzle-adapter'

export const { handlers, auth, signIn, signOut } = NextAuth({
  adapter: DrizzleAdapter(db),
  session: { strategy: 'jwt' },     // JWT 会话（无状态，便于多实例）
  providers: [
    Credentials({
      credentials: { username: {}, password: {} },
      async authorize(creds) {
        // 查 user、bcrypt.compare、返回 { id, username, isSuperAdmin }
      },
    }),
    // ★ 预留：未来加 GitHubProvider / 企业微信 / OIDC
  ],
  callbacks: {
    jwt: async ({ token, user }) => {
      if (user) { token.isSuperAdmin = user.isSuperAdmin; token.username = user.username }
      return token
    },
    session: async ({ session, token }) => {
      session.user.id = token.sub!
      session.user.isSuperAdmin = token.isSuperAdmin as boolean
      // activeEnterpriseId 通过单独查询注入（不进 token，切换更灵活）
      return session
    },
  },
  pages: { signIn: '/login' },
})
```

**扩展类型**（`src/types/next-auth.d.ts`）让 `session.user` 带 `id`、`isSuperAdmin`。

### 5.4 任务队列：从 in-process 到 Redis

旧项目的 `TaskQueue` 是 in-process 单例，**无法水平扩展**。新项目改为 **Redis-backed 队列**：

- **队列结构**：Redis List/Stream 存待处理任务；ZSet 存超时探测；Hash 存任务状态。
- **消费者**：可以是 Next.js 后台 Route（`/api/cron/process-queue`，由 cron 触发），或独立的 Node worker 进程（Docker Compose 单独服务）。**M1 先用 cron Route，M3 视情况拆 worker。**
- **前端轮询**：前端通过 `/api/tasks/[id]/result` 轮询，或用 SSE 推送（M2+）。
- **原子性**：积分扣减在 PG 事务（见 4.2），任务入队在事务外（避免长事务）；退款同样走 PG 事务。

### 5.5 统一创作页与 Pulse 对话框

**页面合并**（决策 D13）：

- 原 `/`（自由创作）+ `/canvas`（项目创作）→ 合并为单一 `/create`；
- 去掉所有 `source === 'creative' | 'project'` 分支，统一 `source = 'create'`；
- 模型查询统一用 `visibleInCreate = true`（合并旧 `visible_in_generate` + `visible_in_canvas`）；
- 计费只扣企业积分池（无 creative/project 区分）。

**Pulse 光效对话框**（参考 Jakub Antalik border-beam）：

- Pulse 是 Jakub Antalik `border-beam` 组件的 **Pulse 变体**——围绕元素的呼吸式发光边框动画（`@property --shine-angle` + conic-gradient，旧项目 `index.css` 已有 `shine-border` 实现）；
- 我们在 `src/components/create/border-beam.tsx` 实现一个 Pulse 变体，包裹 **AI 生图对话框**（`generate-dialog.tsx`），让对话框在"等待生成"时呈现呼吸光效，生成完成时光效收敛；
- 对话框内部结构沿用 shadcn `dialog` + `composer`（旧项目已用），底部展示模型选择、尺寸选择、参考图、积分消耗预估。

### 5.6 AI 适配器迁移策略（不重写算法，只换外壳）

旧项目 `queue.ts` 里的 `callGRSAPI` / `callJimengAPI` 算法成熟，**业务逻辑原样搬迁到 `src/lib/ai/grs.ts` / `jimeng.ts`**，只做三处适配：

1. 数据库访问从 `better-sqlite3` 同步 `query()` 换成 Drizzle 异步 `db`；
2. 队列状态写入从内存换成 Redis；
3. `image-model-config.ts` 的请求体构造逻辑几乎不变。

---

## 六、与旧项目的对照改进（响应"检查不合理处"）

| 旧项目问题 | 改进方案 |
|---|---|
| **无 ORM**，手写 SQL + 动态 `ALTER TABLE` 补字段（647 行 migrate.ts） | Drizzle ORM + 版本化迁移（`drizzle-kit generate/migrate`），schema 即文档 |
| **单租户**，无企业/组织概念 | 全表 `enterprise_id` 行级隔离 + 三层角色 |
| **两种积分**（creative/project）+ 遗留 `credits` 字段混乱 | 统一单一积分 + 强一致流水表 |
| **前端 localStorage 存 token/积分**，XSS 风险 + 多标签不同步 | Auth.js httpOnly cookie session；积分实时查 DB/Redis |
| **`allowed_pages` 仅前端校验**，后端不强制（鉴权缺口） | 后端每个 Action/Route 强制角色 + 企业校验 |
| **in-process 队列**，无法水平扩展 | Redis-backed 分布式队列 |
| **next-themes 装了但没挂 Provider**，暗色不可切换 | `ThemeProvider` 正确挂载，默认 dark |
| **`frontend/@/` 目录**（误把别名当字面路径创建的脏目录） | 新项目用 Next.js 标准 alias，杜绝此类问题 |
| **遗留冗余 DB 文件**（`database.sqlite`/`data.db`/`hanxing.db`） | 单一 PG 数据库，无冗余 |
| **自由创作/项目创作大量重复代码** | 合并为单一创作页 + 提取共享组件 |
| **无类型安全的 API 层**（前端 `apiFetch` 字符串拼接） | Server Actions + zod，前后端共享类型 |

---

## 七、UI 模板落地细节

### 7.1 sidebar-08（主页侧边栏）

执行 `npx shadcn@latest add sidebar-07`（含 nav-user、nav-main、team-switcher、site-header）+ `npx shadcn@latest add sidebar-08`（在 07 基础上加 collapsible 折叠能力）。落地后改造：

| 模板组件 | 改造点 |
|---|---|
| `team-switcher.tsx` | → 改为 `enterprise-badge.tsx`（**纯展示，非切换器**，R5 用户单一归属）；显示当前企业 logo + 名称；超管显示"平台管理"并可点击进 `/platform` |
| `nav-main.tsx` | 菜单项：创作 / 资产管理 / 批量生图 / 商品主图；按 **`enterprise.enabledModules` ∩ `group.allowedPages`** 双重过滤（D22+D21） |
| `nav-user.tsx` | 显示用户名（昵称）+ 企业角色（owner/admin/member）+ 权限组名；底部"退出登录" |
| `nav-secondary.tsx`（管理员入口） | 企业管理员（owner/admin）额外看到"企业管理"二级菜单；超管看到"平台管理"二级菜单 |
| 侧边栏底部 | 显示当前企业剩余积分（单一积分，醒目展示） |

### 7.2 login-04（登录页）

执行 `npx shadcn@latest add login-04`。落地策略（响应"没有的功能保持模板样式由后期指导调整"）：

| 模板元素 | v1 处理 |
|---|---|
| 邮箱+密码表单 | **改为用户名+密码**（沿用旧项目账号体系） |
| "Forgot your password?" 链接 | **保留占位**，v1 不实现找回（沿用旧项目"联系管理员"tooltip） |
| GitHub/Google 社交登录按钮 | **保留按钮样式**，v1 点击提示"暂未开放，请联系管理员"，v2 接入真实 OAuth |
| "Don't have an account? Sign up" | **保留样式**，提示"内部系统，联系管理员开通" |
| 右侧背景图/品牌区 | 沿用旧项目瀚星品牌 logo + 暗色背景 |

### 7.3 黑夜模式落地

- `src/app/layout.tsx` 包裹 `<ThemeProvider attribute="class" defaultTheme="dark" enableSystem={false}>`；
- `<html lang="zh-CN" className="dark" suppressHydrationWarning>` 强制默认；
- globals.css 完整定义 `.dark` 变量块（沿用旧项目已写好的暗色 token）。

---

## 八、环境变量与配置

### 8.1 `.env.example`（完整模板）

```bash
# ══════════ 应用 ══════════
NODE_ENV=development
NEXT_PUBLIC_APP_NAME=瀚星 Super Image
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ══════════ 数据库 ══════════
POSTGRES_USER=hanxing
POSTGRES_PASSWORD=change_me_in_prod
POSTGRES_DB=hanxing_super
DATABASE_URL=postgresql://hanxing:change_me_in_prod@localhost:5432/hanxing_super

# ══════════ Redis ══════════
REDIS_URL=redis://localhost:6379

# ══════════ 认证 ══════════
# Auth.js: 至少 32 字符随机串，生产必改
AUTH_SECRET=generate_with_openssl_rand_hex_32
# Auth.js v5 Trusted Origins（防 host header 攻击）
AUTH_TRUST_HOST=true

# ══════════ 加密（API Key / COS SecretKey 落库加密）══════════
# 32 字符密钥；生产从密钥管理服务获取
ENCRYPTION_KEY=change_me_32_chars_min_aaaaaaaa

# ══════════ 初始超管（首次启动种子） ══════════
SUPERADMIN_USERNAME=superadmin
SUPERADMIN_PASSWORD=             # 留空则启动时随机生成并打印控制台

# ══════════ 腾讯云 COS（可选，默认本地存储）══════════
COS_SECRET_ID=
COS_SECRET_KEY=
COS_BUCKET=hanxing-super-xxxx
COS_REGION=ap-guangzhou
COS_BASE_URL=
COS_IMAGE_PREFIX=image/

# ══════════ 开发期代理（仅开发机用）══════════
# 本机 7897 端口已运行 Clash 类代理（HTTP+SOCKS 混合）
# 开发时访问 npm registry / shadcn registry / GitHub 需走代理
HTTPS_PROXY=http://127.0.0.1:7897
HTTP_PROXY=http://127.0.0.1:7897
NO_PROXY=localhost,127.0.0.1,postgres,redis

# ══════════ AI 任务队列（可选 cron 触发）══════════
CRON_SECRET=                          # /api/cron/* 的 Bearer token
QUEUE_POLL_INTERVAL_MS=2000
```

### 8.2 Docker Compose 编排

```yaml
# docker-compose.yml（要点）
services:
  postgres:
    image: postgres:16-alpine
    environment: [POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB]
    volumes: ["pg_data:/var/lib/postgresql/data"]
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "$$POSTGRES_USER"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    command: redis-server --save 60 1 --loglevel warning   # 开启 AOF/RDB 持久化
    volumes: ["redis_data:/data"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  app:
    build: .
    depends_on:
      postgres: { condition: service_healthy }
      redis: { condition: service_healthy }
    env_file: .env
    ports: ["3000:3000"]
    volumes: ["./uploads:/app/uploads"]
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

  # 可选：独立队列 worker（M3 引入，水平扩展生图消费）
  # worker:
  #   build: .
  #   command: pnpm worker
  #   depends_on: [postgres, redis]
  #   env_file: .env
  #   restart: unless-stopped

volumes: { pg_data: {}, redis_data: {} }
```

### 8.3 运维保障（★ 新增）

| 项 | 策略 |
|---|---|
| **健康检查** | `/api/health` 端点（沿用旧项目），检查 PG 连接 + Redis 连接；docker-compose 三个服务均配 healthcheck |
| **PG 备份** | 生产环境每日 `pg_dump` 到 `backups/` 目录（宿主机 cron：`0 3 * * * pg_dump ... | gzip > backups/pg_$(date +%F).sql.gz`），保留 14 天 |
| **Redis 持久化** | 开启 RDB（`save 60 1`）+ AOF（视数据重要性，M3 评估），保证队列任务不丢 |
| **uploads 备份** | 若用本地存储，`uploads/` 目录纳入宿主机备份计划；推荐生产用 COS（云厂商负责持久性） |
| **日志** | 应用日志走 stdout（Docker logs 收集）；`login_logs`/`api_call_logs`/`credit_transactions` 落 PG 做审计；可接 ELK/Loki |
| **监控** | MVP 用 docker-compose 内置 + `/api/health`；后续可接 Prometheus + Grafana（监控队列长度、生图成功率、企业积分水位） |

### 8.3 开发期代理（端口 7897）

- 本机 7897 已确认是 **Clash 类混合代理**（HTTP+SOCKS），监听 `127.0.0.1:7897`；
- `.env` 中已配 `HTTP_PROXY`/`HTTPS_PROXY` 指向它；
- pnpm、shadcn CLI、Drizzle Kit 会自动读取这两个环境变量；
- **生产环境容器内不需要代理**（通过 `NO_PROXY` 排除内部服务）。

---

## 九、里程碑计划（基础架构优先）

### M0 · 项目初始化（0.5 天）

- [ ] 在 `Hanxing-Super-image/` 初始化 pnpm + Next.js 15（`pnpm create next-app`，TS、Tailwind、App Router、src 目录）；
- [ ] 配置 `.npmrc`（开发期 registry + proxy）、`.gitignore`、`tsconfig` 严格模式；
- [ ] 安装 shadcn：`pnpm dlx shadcn@latest init`（base color neutral、CSS variables、`src/` alias）；
- [ ] 添加基础组件：`button input card dialog dropdown-menu sheet ...`；
- [ ] 添加模板：`sidebar-07 sidebar-08 login-04`；
- [ ] 接入 `next-themes`，强制 `dark` 默认，验证 sidebar-08 在黑夜模式下显示正常。

### M1 · 全栈骨架 + 多租户地基（2 天）

- [ ] Docker Compose 起 PG + Redis（开发期本地）；
- [ ] Drizzle 接入：`schema/auth.ts`（users/accounts/sessions，**含 enterpriseId/enterpriseRole/groupId**）、`schema/enterprise.ts`（enterprises/permissionGroups）、`schema/credits.ts`；
- [ ] 4.5 节索引全部建好；
- [ ] `drizzle-kit generate` + `push`，生成首个迁移；
- [ ] Auth.js v5：Credentials Provider + DrizzleAdapter + 自定义登录页（套 login-04）；
- [ ] 种子脚本：创建平台超管（无企业）+ 一个默认企业 + 该企业默认权限组；
- [ ] `getCurrentUserContext()`（返回 user + enterprise + group 三元组）辅助函数；
- [ ] Next.js middleware：路由守卫（超管/企业角色/模块开关三级粗粒度）；
- [ ] **验收**：能登录、能进入 dashboard 看到 sidebar-08（企业标识展示用户归属企业）。

### M2 · 企业与积分管理（1.5 天）

- [ ] Server Actions：`platform.createEnterprise`（含种子默认权限组 + enabledModules 配置）、`platform.createUserAndAssign`（超管建账号+分配企业，D19）、`platform.rechargeCredits`；
- [ ] Server Actions：`admin.createMember`/`admin.removeMember`/`admin.changeRole`/`admin.assignGroup`（企业管理员管本企业成员，D19）；
- [ ] `credits-service.ts`：原子扣减（PG 行锁事务）、退款、流水写入；
- [ ] 平台管理页（`/platform/enterprises`）：CRUD 企业、配置 enabledModules、充值、查看成员；
- [ ] 平台管理页（`/platform/users`）：超管建用户、分配企业、改角色；
- [ ] 企业管理页（`/admin/users`）：企业管理员增删成员、改角色、分配权限组；
- [ ] 企业管理页（`/admin/groups`）：权限组 CRUD（D21）；
- [ ] sidebar-08 底部展示企业积分余额；
- [ ] **验收**：超管建企业+充值+建用户分配；企业管理员邀成员+设权限组；成员看到企业积分与受限菜单。

### M3 · 统一创作页（2 天）

- [ ] `schema/models.ts`、`schema/tasks.ts`、`schema/system.ts` 落地；
- [ ] `lib/ai/grs.ts` + `jimeng.ts` + `image-model-config.ts`（从旧项目迁移算法）；
- [ ] `lib/queue/task-queue.ts`（Redis-backed，cron Route 消费）；
- [ ] 创作页 `/create`：模型选择、尺寸选择、参考图、Pulse 光效对话框、提交；
- [ ] Server Action `create.submitTask`：校验模型在 `group.allowedModels` + 企业模块开关 → 扣积分 → 入队 → 返回任务 id；
- [ ] 任务结果轮询/SSE；
- [ ] 三层并发上限生效（企业/组/模型取最小）；
- [ ] **验收**：能选模型（受权限组限制）、输入提示词、看到 Pulse 光效、生成成功、积分扣减、流水落库；**并发扣减集成测试通过**（多请求同时扣同企业不超扣）。

### M4 · 资产管理 + 用户设置（1 天）

- [ ] `/assets`：跨来源图片画廊（按日期分组、瀑布流/网格、收藏）；
- [ ] `/settings`：个人资料、改密码、切换默认企业。

### M5 · 批量生图工作台（2.5 天）

- [ ] `schema/workspace.ts` 全套表 + 工作台组件迁移；
- [ ] `workspace-ai.ts` 提示词裂变/翻译/深化；
- [ ] Redis chat 队列；
- [ ] `/workspace` 全流程。

### M6 · 商品主图（1.5 天）

- [ ] `schema/product.ts` 全套表 + 组件迁移；
- [ ] `/product` 单图模式 + 模板模式。

### M7 · 收尾（1 天）

- [ ] 系统设置（存储后端切换、队列阈值）；
- [ ] 平台/企业操作日志；
- [ ] Dockerfile 多阶段构建 + 生产 docker-compose 验证；
- [ ] README + 部署文档。

**预估总工期：约 14 个工作日**（单人全职；M1/M2 因权限组、成员管理、模块开关较 v1.0 略增；多 agent 并行可压缩到 9 天）。

---

## 十、开发规范

### 10.1 代码规范

- **TypeScript strict** 模式，禁用 `any`（确需时用 `// eslint-disable-next-line` 标注原因）；
- **优先 shadcn 组件**：能用 `Button`/`Dialog`/`Table` 等已有组件的，**禁止**自己写 `<button>`/原生表格；
- **Server Action 命名**：动词开头，如 `submitTask`、`rechargeCredits`、`createEnterprise`；
- **zod schema 前后端共享**：放 `src/types/` 或就近 schema 文件，Server Action 入参用 `.parse()`；
- **Drizzle 查询**：禁止裸 SQL 字符串拼接（防注入），复杂查询用 `sql` 模板标签。

### 10.2 多租户铁律

1. **所有业务查询必须带 `enterpriseId`**，通过 `getCurrentEnterpriseScope()` 统一注入；
2. **所有 Server Action 入口校验 `(user, enterprise, role)`**；
3. **任何"全局查询"必须显式声明并 code review**（仅超管场景允许）。

### 10.3 Git 规范

- 分支：`main`（受保护）、`dev`、`feat/xxx`、`fix/xxx`；
- Conventional Commits：`feat(create): add pulse dialog`、`fix(credits): lock row on deduct`；
- 每个 PR 必须通过 `pnpm typecheck && pnpm lint`。

### 10.4 文件归属铁律（防客户端打包服务端代码）

- 服务端专用代码（DB、Redis、AI 适配器、加密、Server Action）放 `src/server/` 或 `src/lib/` 下的明确服务端模块；
- **严禁**在 `"use client"` 文件中 `import` 任何 `src/db/*` 或 `src/lib/ai/*`；
- Server Action 文件顶部必须 `"use server"`。

### 10.5 多租户命名规范（★ 新增，防数据串户）

**存储路径**（本地 + COS）必须带企业维度，禁止平铺：

```
本地：uploads/<enterpriseId>/image/<yyyy>/<mm>/<uuid>.<ext>
      uploads/<enterpriseId>/thumb/...
COS： <COS_IMAGE_PREFIX><enterpriseId>/image/<yyyy>/<mm>/<uuid>.<ext>
```

**Redis Key 命名**（全部带 `enterpriseId` 前缀，避免 key 冲突）：

```
hanxing:ent:<enterpriseId>:queue:tasks          # 任务队列（List）
hanxing:ent:<enterpriseId>:task:<taskId>:status # 单任务状态（Hash）
hanxing:ent:<enterpriseId>:concurrent           # 当前并发计数
hanxing:ent:<enterpriseId>:rate:generate        # 生图限流计数
hanxing:login:rate:<ip>                          # 全局登录限流（不带企业，按 IP）
hanxing:session:<userId>                         # 会话缓存
```

**并发控制的租户维度**（★ 新增）：

- 旧项目 `max_concurrent` 仅按**模型**限；
- 新项目三层并发上限：`enterprise.maxConcurrent`（企业总并发）≥ `group.maxConcurrent`（权限组并发）≥ 模型 `models.maxConcurrent`；
- 计数器放 Redis（`INCR`/`DECR` + 过期兜底），队列调度时取三者最小值。

### 10.6 测试策略（★ 新增，企业级应用必备）

| 层 | 工具 | 覆盖范围 | MVP 目标 |
|---|---|---|---|
| **单元测试** | Vitest | `lib/crypto`、`lib/ai/image-model-config`（请求体构造）、`services/credits-service`（扣减/退款逻辑）、`lib/auth/permissions`（角色判定） | M2 起核心服务必须有测试 |
| **集成测试** | Vitest + 真 PG/Redis（testcontainers） | 积分扣减并发（多请求同时扣同一企业，验证行锁）、多租户隔离（A 企业查不到 B 企业数据） | M2 必须有"并发扣减"和"租户隔离"两个关键集成测试 |
| **E2E** | Playwright | 登录流程、提交生图、企业模块开关生效 | M3 起关键链路 |
| **类型/lint** | tsc + eslint | 全量 | 每次 PR 强制 |

**测试铁律**：积分扣减的并发正确性（防止超扣）**必须**有集成测试覆盖，这是计费系统的红线。

### 10.7 安全审计（★ 新增）

- **登录限流**：沿用旧项目策略，Redis 计数，每 IP 15 分钟 10 次失败后锁定；
- **`login_logs` 审计表**：记录 userId（可空，失败登录）、ip、userAgent、登录时间、是否成功；企业管理员可查本企业、超管可查全局；
- **API Key 落库加密**：沿用旧项目 AES-256-GCM（`ENCRYPTION_KEY`）；
- **图片代理防 SSRF**：`/api/image/proxy` 必须校验目标 host 白名单（COS 域名 + AI 服务域名）；
- **`AUTH_SECRET` / `ENCRYPTION_KEY` 启动校验**：服务拒绝使用默认值/空值启动（沿用旧项目）。

---

## 十一、风险与开放问题

| # | 风险/问题 | 当前处理 | 状态 |
|---|---|---|---|
| R1 | Auth.js v5 的 DrizzleAdapter 对自定义 `user` 表（带 `isSuperAdmin`/`enterpriseId` 等扩展字段）的兼容性 | 用 v5 最新文档的 schema 映射方式，扩展字段在 callback 里注入 | M1 验证 |
| R2 | Next.js cron Route 触发队列的可靠性（Vercel 限制，自托管无此问题） | 自托管 Docker，用宿主机 cron 或容器内 supercronic 触发 `/api/cron/process-queue` | M3 选型 |
| R3 | Pulse 光效在低端设备性能 | framer-motion + CSS `@property`，可降级为静态边框 | M3 调优 |
| R4 | 旧项目"模型可见性"有 4 个字段，合并后历史配置如何映射 | 全新数据（D15 不迁移），直接用新字段 | 已解决 |
| R5 | 用户名是否全局唯一、能否跨企业 | ✅ **已确认**：用户名全局唯一（登录账号），用户单一归属企业不可跨企业，昵称可重复 | **已关闭** |
| R6 | 是否开放企业自助注册 | ✅ **已确认**：v1 不开放，仅超管创建企业 | **已关闭** |
| R7 | 用户跨企业调换的需求 | 当前禁止跨企业；如确需，由超管改 `users.enterpriseId`（其历史任务数据归属问题见 R8） | 待 v2 评估 |
| R8 | 用户被调换企业后，历史生成任务的归属 | 任务表的 `enterpriseId` 记录的是**生成时的企业**，不随用户调动而变（审计一致性）；资产页按"任务所属企业"过滤，调动后用户看不到原企业资产 | 默认采纳，可调 |
| R9 | 企业停用（suspended）后，成员能否登录 | 成员可登录但企业相关操作全部 403（企业级中间件拦截），超管可见 | M2 实现 |
| R10 | 并发计数器在 Redis 故障时的降级 | Redis 不可用时，生图队列降级为"拒绝新任务 + 告警"，避免超卖 | M3 实现 |

---

## 十二、下一步行动

本手册已经产品负责人确认（R5/R6 已闭合）。下一步进入 **M0（项目初始化）**：

1. 在 `Hanxing-Super-image/` 执行 `pnpm create next-app`；
2. 安装 shadcn 并添加 `sidebar-07 sidebar-08 login-04` + 基础组件；
3. 起本地 PG + Redis（docker-compose）；
4. 搭 Drizzle schema 骨架（auth/enterprise/credits/permissionGroups）与 Auth.js；
5. **M0/M1 完成后向产品负责人演示**：黑夜模式登录 + sidebar-08 主壳 + 企业标识展示（用户归属企业）。

---

**附：关键参考文件路径（旧项目，迁移时对照）**

- 旧 schema：`Hanxing-image/backend/src/db/migrate.ts`
- 旧 AI 队列：`Hanxing-image/backend/src/services/queue.ts`
- 旧 GRS/即梦适配：`Hanxing-image/backend/src/lib/image-model-config.ts`
- 旧创作页：`Hanxing-image/frontend/src/pages/GeneratePage.tsx`、`CanvasPage.tsx`
- 旧侧边栏：`Hanxing-image/frontend/src/layouts/MainLayout.tsx`
- 旧架构报告：`Hanxing-image/项目架构分析报告.md`
- Pulse 光效参考：https://beam.jakubantalik.com/ （GitHub: https://github.com/JakubAntalik/border-beam）

---

## 十三、样机渲染模块（对接外部 psd-render-api）

> 2026-09 增补：样机渲染（mockup）模块架构说明。渲染服务为企业级外部实例
> （地址/密钥/单价按企业在超管平台 `system_setting(key="mockup")` 配置），
> 模板目录天然按企业隔离。

### 13.1 概念与数据模型（`src/db/schema/mockup.ts`）

| 概念 | 存储 | 说明 |
|---|---|---|
| 小模板 | 外部 psd-render-api 的 PSD 模板版本（`tpv_`） | 上传/绑定编辑/发布直连外部；归属人（`ownerUserId`）与可见性（public/private）由外部按 `X-User-Id/X-User-Admin` 透传判定 |
| 大模板（套组） | 本地 `mockup_group` | `ownerUserId` 归属人 + `visibility`（public/private）+ `autoGenerated`（单小模板自动建组标记）；成员表 `mockup_group_item` 钉住版本并快照绑定定义（含 `role: "background"` 背景标记合并） |
| 卡片 | 本地 `mockup_card` | 个人渲染实例：`bindingConfig` 存每个小模板的图片/文字配置 |
| 批量替换批次 | 本地 `mockup_batch` | 小模板级批量套版：`fixedConfig` 固定绑定 + 逐任务轮换图片/文字；任务计数由任务表实时聚合（不冗余存储） |
| 背景标记 | 本地 `mockup_template_extra` | 小模板本地扩展属性：`backgroundBindingIds`（外部 API 无此概念，网页侧独有） |

渲染任务复用 `generation_tasks`（`taskType=mockup`、无 AI 模型）：卡片渲染带
`cardId/groupItemId`，批量替换带 `batchId/batchIndex/batchLabel`，外部 jobId 等
上下文走 `templateInfo`（结构见 `src/lib/mockup/task-info.ts`）。

### 13.2 页面与权限

- `/mockup` **模板渲染**：页内 Tab（模板渲染/批量替换）+ 顶栏「创建卡片」
  （按大模板/按小模板选，小模板自动建单成员套组）/「渲染」/「模板管理」弹窗。
- `/mockup/batch` **批量替换**：选已发布小模板 → 配置替换源（图片文件夹轮换 /
  CSV 文字轮换 / 背景与固定绑定，支持图库选图与 AI 生成背景）→ 确认扣费提交 →
  批次详情（进度/失败重试/逐张下载/fflate 打包 ZIP）。单批次上限 50 张。
- 模板管理弹窗：「大模板 / 小模板」两个子 Tab，支持名称搜索、归属人显示、
  可见性切换、上传 PSD（上传时选公开/私有）、绑定编辑（含背景标记）。
  已发布版本的绑定结构外部 API 不可改：编辑器保存时服务端先比对提交绑定
  与最新版本绑定（归一化、顺序无关），仅背景标记变化 → 跳过外部保存发布、
  只落本地扩展表；结构有变且已发布 → 报错引导重新上传 PSD（草稿走原
  保存+发布流程）。

**权限规则（所有成员可创建模板）**：

- 上传 PSD/创建大模板：全体成员；归属人管理自己的（改名/可见性/删除/编辑绑定）；
  企业管理员（owner/admin）可管理本企业全部。
- 可见性：小模板由外部按透传用户判定；大模板本地过滤 `public OR owner=me`
  （管理员不过滤）。**公开大模板强制所有成员小模板均为公开**（创建/更新/切换
  可见性时校验，防止他人渲染踩私有模板 403）。
- 建卡/渲染/批量：登录 + 企业上下文（卡片与批次为个人维度）。

### 13.3 背景标记与 AI 生图

- 绑定编辑器可勾选「背景」（本地扩展表持久化；图层名含「背景/bg/background」
  的像素图层默认勾选并标记）。背景绑定在替换弹窗置顶展示。
- 替换弹窗/批量替换页对背景绑定提供「AI 生成」入口：选样机页可用生图模型 →
  提交 `generationTask(taskType=normal, source=mockup)` 入统一 Redis 生图队列
  （按模型单价扣费）→ 弹窗内轮询完成 → 自动收录「我的上传」并回填绑定。
  AI 生图计费与渲染积分（`costPerRender`）相互独立。

### 13.3b 方块操作菜单与 AI背景/AI渲染（2026-09 增补）

- 点击方块弹出 112px 宽操作菜单（portal + fixed，不随卡片列表裁剪）：
  **替换图层**（打开绑定弹窗）/ **AI背景** / **AI渲染** / **查看原图**
  （公共 ImageViewer）/ **查看对比**；不满足前提的项灰显并注明原因。
- **生成配置**（顶栏按钮，同批量生图交互）：选择 AI 用的模型与尺寸
  （`listMockupModelsAction` 过滤 `visibleInMockup`），localStorage 持久化。
- **模型可见性**：模型表新增 `visibleInMockup`（默认 false），企业/平台模型
  表单勾选「样机渲染」，列表页显示「样机」徽章。
- **AI背景**（一键）：以方块渲染原图为参考图（服务端自查最近 completed 渲染
  任务）、提示词取平台模板（`product_prompt_template` 场景 `mockup.ai_background`
  ，超管平台「样机提示词」页维护，缺省回退 `src/lib/mockup/prompt-defaults.ts`），
  固定生成 1 张；完成后 `applyMockupAiBackgroundAction` 落地：原子认领
  （templateInfo.appliedAt 幂等）→ 填入卡片背景绑定 → 自动重渲染该样机
  （计费 costPerRender）。浏览器中途关闭时，页面加载按
  `getMockupPageDataAction().pendingAiApplies` 兜底续做（生成由 worker 执行，
  不受浏览器影响）。
- **AI渲染**：输入提示词，以渲染原图为参考图生成 1 张（不做重渲染）。
- **AI 结果存储**：复用 generationTasks（taskType=normal、source=mockup、
  templateInfo.kind="mockup-ai"，含 aiKind/cardId/groupItemId/refImageUrl 快照），
  页面数据按方块聚合最近 8 张；有 AI 结果的方块显示**流光边框**（BeamWrapper
  · border-beam colorful，与批量生图卡片生成态同款，非蓝色、无徽标；深色模式
  显式传 theme=dark 并加强 strength，因其 auto 按浏览器 prefers-color-scheme
  猜主题与应用 class 主题不同步；外层 -m-px 补偿 + 内层 p-px 垫层把光束环
  外扩到紧贴缩略图边框外侧约 1px，朝外发光不受缩略图浅色内容影响；strength
  深色 2.2 / 浅色 1.5 增强；方块内右上角另有蓝色 Sparkle 单星图标，无底衬），
  AI 生图中的方块整格变为 ig-
  纯加载动画（无文字/进度，服务端 aiGenerating + 提交后轮询双来源），缩略图
  始终显示渲染原图，无悬浮标题提示。渲染轮询需清理「上轮在途、本轮消失」
  任务的 overlay 残留（服务端只返回在途任务，不清会导致方块永远加载中）。
- **查看对比**：全屏弹窗（同 ImageViewer 样式，右上角圆形关闭+下载）；
  `ImageCompareSlider`（clip-path 拖动分割，顶部左右胶囊角标）左=渲染原图快照、
  右=AI 图；底部 AI 结果正方形缩略图条点击切换对比对象。

### 13.4 渲染与批量链路

- 卡片渲染：`renderInternal` 校验必填 → 建任务 → 整批扣费 → 逐任务素材中转
  （本项目存储 URL → 外部 assetId，`mockup_external_asset` 缓存）→ 提交外部
  render job（幂等键=taskUuid）；失败单张退款。
- 批量替换：`submitMockupBatchAction` 同构，第 i 个任务 = 第 i 张图（多个轮换
  图片绑定文件数必须一致）+ CSV 第 i 行文字 + 固定绑定；批次详情轮询
  `getMockupBatchStatusAction`（顺带推进在途任务）。
- 失败重试（`retryBatchTaskAction`）：重新扣费 → **立即写入 creditsCharged**
  （refundFailedTask 按其原子退款）→ 新幂等键重提交。
- 状态收敛双通道：前端轮询（5s）+ worker `syncMockupJobs`（2s），
  终态条件更新保证幂等不双退。

---

_本手册为活文档，实施过程中如有调整，请在此处追加更新记录。_
