node_modules
.next
.git
.gitignore
*.md
.env*
docker-compose*.yml
Dockerfile
.dockerignore
coverage
uploads
backups
*.sqlite
*.db
.pnpm-store
# 注意：不能排除 drizzle/meta——migrate 服务按 journal+snapshot 应用迁移，
# 缺 snapshot 会让 drizzle-kit 状态分叉（builder 内也严禁再 db:generate）
